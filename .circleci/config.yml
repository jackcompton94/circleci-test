version: 2.1

jobs:
  simulate_lengthy_process:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Start
          command: echo "Job started at $(date)"
      - run:
          name: Sleep for 90s
          command: |
            echo "Sleeping for 90s..."
            sleep 90
      - run:
          name: Done
          command: echo "Job finished at $(date)"

workflows:
  test_concurrency:
    triggers:
      - schedule:
          cron: "* * * * *"  # Every minute (✔️ POSIX compliant)
          filters:
            branches:
              only: main
    concurrency:
      group: test-concurrency-demo
      cancel-in-progress: true
    jobs:
      - simulate_lengthy_process

